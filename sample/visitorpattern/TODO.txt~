
[TODO]

1. array -> Parser should support array type, indexing, and allocation.
2. constructor parameters -> Parser should support this.
3. new parameters -> Parser should support this.
4. constructor -> Parser should support methods without any return type

HashSet -> no parser support is needed.
Iterator -> no parser support is needed.
super -> no parser support is needed. Treate it as a primitive function.
System.out.println -> no parser support is needed. Treat it as a primitive function.

